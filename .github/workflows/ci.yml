name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  # Opcional: fija la versión; si se omite, pnpm/action-setup lee packageManager
  PNPM_VERSION: 11.16.0
  NODE_VERSION: 24.21.0

jobs:
  lint:
    name: Lint + typecheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run lint
      - run: pnpm run typecheck

  # unit:
  #   name: Unit tests
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v4
  #       with:
          # version: ${{ env.PNPM_VERSION }}
  #         run_install: false
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: pnpm
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm run test:unit

  # integration-sqlite:
  #   name: Integration (SQLite)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v4
  #       with:
  #         version: ${{ env.PNPM_VERSION }}
  #         run_install: false
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: pnpm
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm run test:integration
  #       env:
  #         DATABASE_ENGINE: sqlite
  #         DATABASE_URL: ':memory:'

  # integration-postgres:
  #   name: Integration (Postgres)
  #   runs-on: ubuntu-latest
  #   services:
  #     postgres:
  #       image: postgres:16-alpine
  #       env:
  #         POSTGRES_USER: notes
  #         POSTGRES_PASSWORD: notes
  #         POSTGRES_DB: notes_test
  #       ports: ['5432:5432']
  #       options: >-
  #         --health-cmd="pg_isready -U notes"
  #         --health-interval=5s
  #         --health-timeout=3s
  #         --health-retries=10
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v4
  #       with:
  #         version: ${{ env.PNPM_VERSION }}
  #         run_install: false
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: pnpm
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm run test:integration
  #       env:
  #         DATABASE_ENGINE: postgres
  #         DATABASE_URL: postgres://notes:notes@localhost:5432/notes_test

  # integration-mongo:
  #   name: Integration (MongoDB)
  #   runs-on: ubuntu-latest
  #   services:
  #     mongo:
  #       image: mongo:7
  #       ports: ['27017:27017']
  #       options: >-
  #         --health-cmd="mongosh --quiet --eval 'db.adminCommand(\"ping\").ok'"
  #         --health-interval=5s
  #         --health-timeout=3s
  #         --health-retries=10
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v4
  #       with:
  #         version: ${{ env.PNPM_VERSION }}
  #         run_install: false
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: pnpm
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm run test:integration
  #       env:
  #         DATABASE_ENGINE: mongodb
  #         DATABASE_URL: mongodb://localhost:27017
  #         DATABASE_NAME: notes_test

  # e2e:
  #   name: E2E (HTTP + GraphQL · SQLite)
  #   runs-on: ubuntu-latest
  #   needs: [unit]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: pnpm/action-setup@v4
  #       with:
  #         version: ${{ env.PNPM_VERSION }}
  #         run_install: false
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: pnpm
  #     - run: pnpm install --frozen-lockfile
  #     - run: pnpm run test:e2e
  #       env:
  #         NODE_ENV: test
  #         PRESENTATION_PROTOCOL: both
  #         DATABASE_ENGINE: sqlite
  #         DATABASE_URL: ':memory:'
  #         JWT_ACCESS_SECRET: test-access-secret
  #         JWT_REFRESH_SECRET: test-refresh-secret
  #         JWT_ACCESS_EXPIRES_IN: 15m
  #         JWT_REFRESH_EXPIRES_IN: 7d
  #         DEFAULT_USER_ROLE: user

  audit:
    name: Security audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      # Falla solo en severidad high+ (audit-level). pnpm audit usa el registro npm.
      - run: pnpm audit --audit-level=high

  # build-image:
  #   name: Build Docker image (smoke)
  #   runs-on: ubuntu-latest
  #   needs: [lint]
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: docker/setup-buildx-action@v3
  #     - uses: docker/build-push-action@v6
  #       with:
  #         context: .
  #         push: false
  #         tags: notes-api:ci
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max